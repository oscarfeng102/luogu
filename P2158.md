# P2158 [SDOI2008] 仪仗队

原题: <https://www.luogu.com.cn/problem/P2158>

```cpp
#include<bits/stdc++.h>
using namespace std;
const int N=114514;
int f[N];

void gcd(int n){
	for (int i = 1; i <= n; i++) {
		f[i]=i;
	} 
	for (int i = 2; i <= n; i++) {
		if (f[i]==i) {
			for (int j = i; j <= n; j+=i) {
				f[j] = f[j] / i * (i-1);
			}	
		}
	}
}

int main(){
	int n;
	cin >> n;
	if (n == 1){
		cout<<0<<endl;
		return 0;
	}
	int ans = 0;
	gcd(n);
	for (int i = 1; i <= n-1; i++){
        ans += f[i];
	}
	cout << (ans + ans + 1) << endl;
	return 0;
}
```
