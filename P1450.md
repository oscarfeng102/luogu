# P1450 [HAOI2008] 硬币购物

原题 <https://www.luogu.com.cn/problem/P1450>

```cpp
#include <bits/stdc++.h>
using namespace std;
const int N=100009;
long long dp[N];
int main() {
	int c[4],d[4];
	for(int i=0;i<4;i++)scanf("%d",&c[i]);
	dp[0]=1;
	for(int i=0;i<4;i++)
		for(int j=c[i];j<N;j++)
			dp[j]+=dp[j-c[i]];
	int t;scanf("%d",&t);
	while(t--){
		for(int i=0;i<4;i++) scanf("%d",&d[i]);
		int s;scanf("%d",&s);
		long long ans=dp[s];
		for(int i=1;i<=15;i++){
			int now=s;
			int tmp=i;
			int ov=0;
			for(int j=0;tmp;j++){
				if(tmp&1)ov^=1,now-=(d[j]+1)*c[j];
				tmp=tmp>>1;
			}
			if(now<0)continue;
			if(ov)ans-=dp[now];
			else  ans+=dp[now];
		}
		cout<<ans<<"\n";
	}
}
```
